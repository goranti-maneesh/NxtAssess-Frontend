import tw from "twin.macro";
import styled from "styled-components";

export const AssessmentRouteCantainer = styled.div``;

export const LoaderAssessmentContainer = styled.div`
	${tw`flex justify-center items-center h-90vh`}
`;
